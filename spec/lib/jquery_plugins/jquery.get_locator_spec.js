// Generated by CoffeeScript 1.3.3
(function() {

  jasmine.getFixtures().fixturesPath = 'fixtures';

  describe('jquery.get_locator', function() {
    beforeEach(function() {
      return loadFixtures('jquery_plugins/jquery.get_locator.html');
    });
    it('should return the first attribute found', function() {
      return expect($('#bar').getLocator(['name', 'id'])).toEqual('foo');
    });
    it('should find the id', function() {
      return expect($('#bar').getLocator(['id'])).toEqual('bar');
    });
    it('should find the name', function() {
      return expect($('#bar').getLocator(['name'])).toEqual('foo');
    });
    it('should find the label', function() {
      return expect($('#bar').getLocator(['label'])).toEqual('Foobar');
    });
    it('should find the text', function() {
      return expect($('#bat').getLocator(['text'])).toEqual('Bat');
    });
    it('should find the value', function() {
      return expect($('#bar').getLocator(['value'])).toEqual('baz');
    });
    return it('should find the image alt attribute', function() {
      return expect($('#bot').getLocator(['imgAlt'])).toEqual('Image foo');
    });
  });

}).call(this);
